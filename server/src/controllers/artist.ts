import {NextFunction, Request, Response} from "express";


export function addArtistController(req: Request, res: Response, next: NextFunction){
    const { name, email, avatar } = req.body;
    
    try{
        // const artist = AR
        
    } catch (ex){
    
    }
    
    
}